const router = require("express").Router()

//app.post("/:thoguhtID/reactions")
//I can add the core properties of the 'thought' by its id, but every time I add a reaction it's an app.post & I need to know which thought ID and what I'm gonna do in the reactions

//app.delete("/:thoughtId/reactions/:reactionID")
//to delete I need to know which thought am I going to remove a reaction ID from

//updating and existing reaction, you could select it by its ID since it exists/has an ID
//app.put("/:thoughtID/reactions/:reactioniD")
module.exports - router
